language: cpp

compiler:
  - clang++
  - g++

env:
  global:
    - "FTP_USER=bnsmith"
    - secure: "DeOkcMhJnlt4ObJq+5WRNajr50uCu7UEbzipwtyw5kqu/qshPl1/AqlA76asvcC6Z905SrgARi+uVVEC+eym19KtnG2l9+j4e3sQejBkn0RK+YGtmjFxxPecSX3OKKzQjtw3QzbXDu77O5QVFSYp8NqF/SPxBpIKEm5Klc3BPc8="

before_install:
  # g++4.8.1
  - if [ "$CXX" == "g++" ]; then sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test; fi

  # clang 3.4
  - if [ "$CXX" == "clang++" ]; then sudo add-apt-repository -y ppa:h-rayflood/llvm; fi

  - sudo apt-get install freeglut3-dev glew1.5-dev libfreetype6-dev libopenal-dev sshpass
  - sudo apt-get update -qq

install:
  # g++4.8.1
  - if [ "$CXX" == "g++" ]; then sudo apt-get install -qq gcc-4.8 g++-4.8; fi
  - if [ "$CXX" == "g++" ]; then sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 90; fi

  # clang 3.4
  - if [ "$CXX" == "clang++" ]; then sudo apt-get install --allow-unauthenticated -qq clang-3.4; fi
  - if [ "$CXX" == "clang++" ]; then export CXX="clang++-3.4"; fi
  - cd ${TRAVIS_BUILD_DIR}


before_script: 
  - cd ${TRAVIS_BUILD_DIR}
  - mkdir build

script: 
  - cd ${TRAVIS_BUILD_DIR}
  - make engine && make editor && make testing && ./build/my-engine-test

after_success:
  - mkdir build_$CXX
  - cp build/* build_$CXX/ 
  #- "curl -i --ftp-create-dirs -T ./build/my-engine -u $FTP_USER:$FTP_PASSWORD sftp://login.ccs.neu.edu:22/~/myengine/"
  - "ssh-keygen -R login.ccs.neu.edu" 
  - echo $SSHPASS
  - echo $TRAVIS_SECURE_ENV_VARS
  - "sshpass -e scp -o 'StrictHostKeyChecking=no' -r ./build $FTP_USER@login.ccs.neu.edu:~/my-engine/"
